info:
  version: v1.0
components:
  parameters:
    BffSenderId:
      $ref: 'https://raw.githubusercontent.com/pagopa/pn-user-attributes/34e510f3033b43cd761afa8a6e1bd2af78eca133/docs/openapi/pn-address-book-common-components.yaml#/components/parameters/senderId'
  schemas:
    BffUserAddresses:
      type: array
      items:
        $ref: '#/components/schemas/BffUserAddress'

    BffUserAddress:
      type: object
      required:
        - addressType
        - recipientId
        - senderId
        - channelType
        - value
      properties:
        addressType:
          type: string
        senderId:
          type: string
          description: senderId or 'default'
        senderName:
          type: string
          description: name of the sender
        channelType:
          $ref: '#/components/schemas/BffChannelType'
        value:
          type: string
          x-field-extra-annotation: "@lombok.ToString.Exclude" # NO EXTERNAL
        codeValid:
          type: boolean
        pecValid:
          type: boolean

    BffChannelType:
      type: string
      enum:
        - EMAIL
        - SMS
        - PEC
        - APPIO
        - SERCQ_SEND

    BffAddressType:
      type: string
      enum:
        - LEGAL
        - COURTESY

    BffAddressVerificationRequest:
      $ref: 'https://raw.githubusercontent.com/pagopa/pn-user-attributes/34e510f3033b43cd761afa8a6e1bd2af78eca133/docs/openapi/pn-address-book-common-components.yaml#/components/schemas/AddressVerification'

    BffAddressVerificationResponse:
      $ref: 'https://raw.githubusercontent.com/pagopa/pn-user-attributes/34e510f3033b43cd761afa8a6e1bd2af78eca133/docs/openapi/pn-address-book-common-components.yaml#/components/schemas/AddressVerificationResponse'